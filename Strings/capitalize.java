package Strings;

public class capitalize {
    public static void main(String[] args) {
        String title="fn fn nvenven   erogvoe";
        String[] arr=title.trim().split("\\s+");
        for (int i = 0; i <arr.length; i++) {
            String s=arr[i];
            char first=s.charAt(0);
            if(Character.isLowerCase(first)){
                s.capi

            }
            
        }
    }
    
}
